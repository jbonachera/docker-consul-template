# Docker image - Consul template

Simple fedora image, with consult-template and supervisord embeded.

"supervisord.conf" and "consul-template.conf" will be added to the image at build (via ONBUILD directive).
